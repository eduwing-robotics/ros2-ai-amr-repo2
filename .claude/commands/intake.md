# /intake

새 요청을 받았을 때 `task-intake-router`를 먼저 적용한다.

## Steps

1. 요청을 `plan`, `implement`, `doc-sync`, `review`, `impact-map`, `subagent-needed`로 분류
2. 다음에 읽을 문서와 호출할 스킬 1~2개 선택
3. `PROJECT-PLAN.md`의 `## Intake Verdict` 갱신
4. 필요 시 `Sub-Agent Opportunities`도 같이 적기
