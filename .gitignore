# ── ROS2 워크스페이스 빌드 산출물 ──
build/
install/
log/
# ── 외부 ROS2 패키지 (urhynix.repos + vcs로 관리) ──
src/turtlebot3/
src/turtlebot3_msgs/
src/DynamixelSDK/
# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
# ── Unity (UNITY) ──
UNITY/Library/
UNITY/Temp/
UNITY/Obj/
UNITY/Logs/
UNITY/Build/
UNITY/Builds/
UNITY/UserSettings/
UNITY/MemoryCaptures/
*.csproj
*.sln
*.unityproj
# ── 웹 client ──
node_modules/
dist/
# ── 시크릿: 절대 커밋 금지 ──
**/SupabaseConfig/supabase.json
**/SupabaseConfig/supabase.json.meta
*.env
.env
*_secret*
*.key
# ── 런타임 데이터 ──
patrol_runs.jsonl
*.bag
*.db3
# ── OS ──
.DS_Store
Thumbs.db
# ── AI 스캐폴딩·내부메모 (로컬 유지, 공개 제외) ──
.claude/
.codex/
.obsidian/
ai-context/
CLAUDE.md
AGENT.md
AGENTS.md
HARNESS-MANIFEST.yaml
